# Tokens · Color

Fuente única de verdad: `05-agentes/component-manifest.json` (sección `tokens.values`).
Esta página se genera desde el manifiesto — no dupliques valores aquí.

**Principio:** el componente apunta al token semántico (`color.action`), nunca al valor crudo.
Cambiar de marca = cambiar `data-brand`, no editar componentes.
